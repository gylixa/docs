# Документация Django на русском языке

Добро пожаловать в проект документации Django на русском языке! Этот проект содержит русскоязычную документацию по Django и инструменты для генерации docset для Dash/Zeal.

## 📋 О проекте

Этот проект предоставляет:
- Русскоязычную документацию по Django
- Инструменты для генерации docset формата
- Возможность удобного просмотра документации в Dash/Zeal

## 🚀 Начало работы

### Предварительные требования

- Python 3.9+
- pip (менеджер пакетов Python)
- make (для сборки документации)
- Git (для клонирования репозитория)

### Установка

1. **Клонируйте репозиторий:**

   ```bash
   git clone https://github.com/hiashyr/docs.git
   cd docs
   ```

2. **Создайте и активируйте виртуальное окружение:**

   ```bash
   # Создать виртуальное окружение
   python -m venv venv

   # Активировать виртуальное окружение
   # На macOS/Linux:
   source venv/bin/activate

   # На Windows:
   venv\Scripts\activate
   ```

3. **Установите зависимости:**

   ```bash
   pip install -r requirements.txt
   ```

   Это установит все необходимые пакеты:
   - Sphinx (для сборки документации)
   - sphinx-rtd-theme (тема для документации)
   - Дополнительные пакеты для функциональности

## 📚 Работа с документацией

### Сборка HTML документации

```bash
make html
```

Эта команда соберет HTML документацию из исходных RST файлов (находящихся в директории `source/`) и разместит результат в директории `build/html/`.

### Ручная сборка и генерация

Если вы хотите сгенерировать docset:

```bash
python generate_docset.py
```

## 📂 Структура проекта

```
docs/
├── source/                  # Исходные RST файлы документации
├── build/                   # Скомпилированная HTML документация
├── My Django Project.docset/ # Готовый docset для Dash/Zeal
├── generate_docset.py       # Скрипт для генерации docset
├── Makefile                 # Makefile для сборки документации
├── requirements.txt         # Зависимости Python
├── README.md                # Этот файл
└── .gitignore               # Файлы для игнорирования Git
```